# Hypeship fork

Kernel keeps `main` synchronized with `QuintinShaw/pi-dynamic-workflows` and
maintains Hypeship-specific behavior on reviewed branches in this fork.

The Workflow Navigator branch adds a headless projection and control seam for
Hypeship's agent runner. Hypeship pins an immutable commit from this repository
when building its VM image.
